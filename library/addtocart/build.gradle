apply plugin: 'com.android.library'
apply plugin: 'realm-android'

buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath gradlePlugins.realmGradlePlugin
    }
}

android {
    compileSdkVersion "$libraryVersions.compileSdk".toInteger()
    buildToolsVersion "$libraryVersions.appBuildTools"

    defaultConfig {
        minSdkVersion "$libraryVersions.minSdk"
        targetSdkVersion "$libraryVersions.targetSdk"
        versionCode "$libraryVersions.addtocartVersionCode".toInteger()
        versionName "$libraryVersions.addtocartVersionName"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation libraries.androidXAppCompat

    // Parceler
    implementation libraries.parcelerApi
    annotationProcessor libraries.parceler

    implementation project(":library:realmmanager")
}